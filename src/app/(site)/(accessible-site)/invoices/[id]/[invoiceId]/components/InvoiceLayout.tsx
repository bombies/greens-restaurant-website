"use client";

type Props = {
    customerId: string,
    invoiceId: string
}

export default function InvoiceLayout({ customerId, invoiceId }: Props) {
    return (
        <div>

        </div>
    )
}